// ignore_for_file: public_member_api_docs, sort_constructors_first
class Push {
  final String titulo;
  final String descricao;

  Push(this.titulo, this.descricao);

  @override
  String toString() => 'Push(titulo: $titulo, descricao: $descricao)';
}
